# leetcode

Accepted solutions, synced from the platform. One folder per problem; mostly C++.

```bash
git pull origin main
```

No monorepo build. Open a problem directory and compile with whatever that folder uses.
